# Feature Roadmap - Project Progress DB

## 📋 目次

1. [ビジョンと目標](#ビジョンと目標)
2. [現在の実装状況](#現在の実装状況)
3. [機能一覧（フェーズ別）](#機能一覧フェーズ別)
4. [開発の進め方](#開発の進め方)
5. [技術的考慮事項](#技術的考慮事項)

---

## 🎯 ビジョンと目標

### ミッション
> 会議議事録という非構造化データをAIで構造化し、プロジェクト管理の生産性を10倍向上させる

### 成功指標（KPI）
- 会議後のタスク整理時間: 30分 → 3分（90%削減）
- リスク見落とし率: 50%削減
- プロジェクト状況把握時間: 5分以内で全体俯瞰

---

## ✅ 現在の実装状況

### 実装済み機能 (MVP)

| カテゴリ | 機能 | 状態 | 備考 |
|----------|------|------|------|
| **認証** | Google OAuth | ✅ 完了 | ドメイン制限対応 |
| **認証** | セッション管理 (JWT) | ✅ 完了 | |
| **入力** | 議事録アップロード (.txt/.md) | ✅ 完了 | |
| **入力** | 会議メタ情報入力 | ✅ 完了 | 日付・タイトル |
| **AI解析** | プロジェクト抽出 | ✅ 完了 | Gemini 2.5 Pro |
| **AI解析** | タスク抽出 | ✅ 完了 | 担当者・期限・優先度・ステータス |
| **AI解析** | リスク抽出 | ✅ 完了 | レベル・元発言保持 |
| **AI解析** | 決定事項抽出 | ✅ 完了 | |
| **AI解析** | 自然言語日付解析 | ✅ 完了 | 「来週金曜」→絶対日付 |
| **表示** | プロジェクト一覧 | ✅ 完了 | |
| **表示** | タスク一覧 | ✅ 完了 | フィルタ・ソート・ページネーション |
| **表示** | リスク一覧 | ✅ 完了 | 統計サマリー・多軸フィルタ |
| **表示** | 決定事項一覧 | ✅ 完了 | |
| **表示** | 会議一覧 | ✅ 完了 | |
| **編集** | タスク編集・削除 | ✅ 完了 | ソフトデリート |
| **編集** | リスク編集・削除 | ✅ 完了 | |
| **編集** | 決定事項編集・削除 | ✅ 完了 | |
| **出力** | CSV エクスポート | ✅ 完了 | プロジェクト/タスク/リスク/決定事項 |

---

## 📦 機能一覧（フェーズ別）

### Phase 1: ダッシュボード強化 ✅ 完了

**目標**: 日常的に使いたくなるダッシュボードに進化

| # | 機能名 | 説明 | 状態 |
|---|--------|------|------|
| 1.1 | **遅延タスクハイライト** | 期限切れタスクを赤で強調表示、専用ビュー | ✅ 完了 |
| 1.2 | **今週期限タスクビュー** | 今週中に期限が来るタスクの専用ビュー | ✅ 完了 |
| 1.3 | **担当者別タスクビュー** | 担当者でグルーピングしたタスク表示 | ✅ 完了 |
| 1.4 | **プロジェクトサマリー強化** | 未完了タスク数、遅延数、リスク数をカードに表示 | ✅ 完了 |
| 1.5 | **検索機能強化** | グローバル検索（タスク/リスク/決定事項横断） | ✅ 完了 |
| 1.6 | **ダッシュボードカスタマイズ** | ウィジェットの表示/非表示、並び替え | ✅ 完了 |

**実装済みコンポーネント**:

```
frontend/
├── components/
│   ├── TasksByOwnerView.tsx        # 担当者別タスクビュー
│   ├── GlobalSearch.tsx            # グローバル検索
│   └── DashboardCustomizer.tsx     # ダッシュボードカスタマイズ
└── app/
    └── page.tsx                    # メインダッシュボード
```

---

### Phase 2: 週次レポート機能 ✅ 完了

**目標**: PMO/マネージャー向けのレポート自動生成

| # | 機能名 | 説明 | 状態 |
|---|--------|------|------|
| 2.1 | **週次サマリー生成** | 今週の遅延/リスク/決定事項をAIで要約 | ✅ 完了 |
| 2.2 | **Top10 遅延/リスクメール** | コピペ可能なメール文面を自動生成 | ✅ 完了 |
| 2.3 | **レポートテンプレート** | カスタマイズ可能なレポート形式 | ✅ 完了 |
| 2.4 | **定期レポート配信** | 週次で自動生成・通知 | ✅ 完了 |

**実装済みコンポーネント**:

```
backend/api/routers/
└── reports.py                      # レポート生成API

frontend/app/
└── reports/
    └── page.tsx                    # レポート画面
```

**APIエンドポイント**:

```
POST /reports/weekly/generate       # 週次レポート生成
GET  /reports/weekly/{week}         # 特定週のレポート取得
GET  /reports/email-draft           # メールドラフト生成
```

---

### Phase 3: 会議間差分検出 ✅ 完了

**目標**: 前回会議からの変化を自動検出

| # | 機能名 | 説明 | 状態 |
|---|--------|------|------|
| 3.1 | **新規タスク検出** | 前回会議後に追加されたタスク | ✅ 完了 |
| 3.2 | **ステータス変更検出** | IN_PROGRESS→DONEなどの変化 | ✅ 完了 |
| 3.3 | **エスカレーションリスク** | LOW→HIGHに上がったリスク | ✅ 完了 |
| 3.4 | **差分ビュー** | 会議間の変化を視覚的に表示 | ✅ 完了 |
| 3.5 | **タスクライフサイクル** | タスクの初出→議論→完了の履歴 | ✅ 完了 |

**実装済みコンポーネント**:

```
backend/api/routers/
└── diff.py                         # 差分検出API

frontend/app/
└── diff/
    └── page.tsx                    # 差分ビュー画面
```

---

### Phase 4: AI強化 ✅ 完了

**目標**: AIをより賢く、対話的に

| # | 機能名 | 説明 | 状態 |
|---|--------|------|------|
| 4.1 | **自然言語クエリ** | 「Project Aの今月期限タスクを見せて」→結果表示 | ✅ 完了 |
| 4.2 | **AIチャットボット** | ダッシュボード内で質問→回答 | ✅ 完了 |
| 4.3 | **次回アジェンダ提案** | 未解決事項からアジェンダ自動生成 | ✅ 完了 |
| 4.4 | **リスクスコアリング** | 曖昧な発言のリスク度を0-1でスコア化 | ✅ 完了 |
| 4.5 | **ボトルネック分析** | 「このプロジェクトの問題点は？」への回答 | ✅ 完了 |
| 4.6 | **フィードバックループ** | ユーザー訂正から学習 | 🔄 検討中 |

**実装済みコンポーネント**:

```
frontend/components/
└── AIChatWidget.tsx               # AIチャットウィジェット

backend/api/routers/
└── ai.py                          # AI対話API

backend/api/services/
└── ai_processor.py                # AI処理サービス
```

**APIエンドポイント**:

```
POST /ai/query                     # 自然言語クエリ
POST /ai/chat                      # チャット対話
POST /ai/agenda/generate           # アジェンダ生成
GET  /ai/analysis/bottleneck       # ボトルネック分析
```

---

### Phase 5: 外部連携 ✅ 完了

**目標**: 会議データの自動取り込み

| # | 機能名 | 説明 | 状態 |
|---|--------|------|------|
| 5.1 | **Google Drive連携** | ドライブから議事録をインポート | ✅ 完了 |
| 5.2 | **Google Docs連携** | Docsから直接テキスト取得 | ✅ 完了 |
| 5.3 | **Google Calendar連携** | 会議情報の自動取得 | ✅ 完了 |
| 5.4 | **Google Meet文字起こし** | Meet録画の文字起こし取り込み | 🔄 検討中 |
| 5.5 | **Zoom連携** | Zoom文字起こし取り込み | 🔄 検討中 |
| 5.6 | **Slack通知** | 高リスク・遅延タスク通知 | ✅ 完了 |
| 5.7 | **Teams通知** | Teams向け通知 | 🔄 検討中 |

**実装済みコンポーネント**:

```
backend/api/
├── integrations/
│   ├── google_drive.py
│   ├── google_docs.py
│   ├── google_calendar.py
│   └── slack.py
└── routers/
    └── integrations.py            # 連携設定API

frontend/app/
└── settings/
    └── page.tsx                   # 設定画面（連携設定含む）
```

---

### Phase 6: 管理機能 ✅ 完了

**目標**: 企業利用に必要な管理機能

| # | 機能名 | 説明 | 状態 |
|---|--------|------|------|
| 6.1 | **ロール管理** | Admin / PM / Member の権限分離 | ✅ 完了 |
| 6.2 | **ユーザー管理** | ユーザー招待・削除・権限変更 | ✅ 完了 |
| 6.3 | **監査ログ** | 操作履歴の記録・表示 | ✅ 完了 |
| 6.4 | **テナント設定** | 用語カスタマイズ、リスクレベル定義 | 🔄 検討中 |
| 6.5 | **データ保持ポリシー** | 古いデータの自動アーカイブ | 🔄 検討中 |

**実装済みコンポーネント**:

```
backend/api/routers/
└── admin.py                       # 管理機能API

frontend/app/
└── admin/
    └── page.tsx                   # 管理画面
```

---

### Phase 7: ヘルススコア ✅ 完了

**目標**: プロジェクトの健全性を数値化

| # | 機能名 | 説明 | 状態 |
|---|--------|------|------|
| 7.1 | **プロジェクトヘルススコア** | 遅延率・リスク数から健全性を計算 | ✅ 完了 |
| 7.2 | **トレンド分析** | 過去n週のスコア推移 | ✅ 完了 |
| 7.3 | **アラート設定** | スコア低下時の通知 | 🔄 検討中 |
| 7.4 | **比較ビュー** | 複数プロジェクトのスコア比較 | ✅ 完了 |

**実装済みコンポーネント**:

```
frontend/app/
└── health/
    └── page.tsx                   # ヘルススコアダッシュボード
```

**スコア計算ロジック**:

```python
def calculate_health_score(project_id: str) -> float:
    """
    ヘルススコア計算 (0-100)
    - 遅延タスク率: -20点 (50%以上で最大減点)
    - 高リスク数: -5点/件
    - 未完了タスク率: -10点 (70%以上で最大減点)
    - 最終更新からの日数: -1点/日 (7日以上で減点開始)
    """
    base_score = 100
    
    overdue_penalty = min(20, overdue_rate * 40)
    high_risk_penalty = min(25, high_risk_count * 5)
    uncompleted_penalty = min(10, max(0, uncompleted_rate - 0.3) * 33)
    stale_penalty = min(15, max(0, days_since_update - 7))
    
    return max(0, base_score - overdue_penalty - high_risk_penalty 
               - uncompleted_penalty - stale_penalty)
```

---

## 🔧 開発の進め方

### 1. 開発フロー

```
1. Issue作成 (機能ごと)
   ↓
2. ブランチ作成 (feature/xxx)
   ↓
3. 実装
   ├── Backend API
   ├── Frontend UI
   └── Tests
   ↓
4. PR → レビュー → マージ
   ↓
5. Staging デプロイ → 動作確認
   ↓
6. Production デプロイ
```

### 2. 開発環境セットアップ

```bash
# Backend
cd backend/api
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
uvicorn main:app --reload --port 8000

# Frontend
cd frontend
npm install
cp .env.example .env.local
npm run dev
```

### 3. テスト戦略

| レイヤー | ツール | カバレッジ目標 |
|----------|--------|----------------|
| Unit (Backend) | pytest | 80% |
| Unit (Frontend) | Jest | 70% |
| Integration | pytest + httpx | 重要フロー |
| E2E | Playwright | ハッピーパス |

### 4. CI/CD パイプライン

```yaml
# 既存の cloudbuild.yaml を活用
triggers:
  - main ブランチ: Production デプロイ
  - staging ブランチ: Staging デプロイ
  - PR: テスト実行のみ
```

### 5. 推奨開発順序

```
Week 1-2:   Phase 1 (ダッシュボード強化)
Week 3-4:   Phase 2 (週次レポート)
Week 5-7:   Phase 3 (会議間差分)
Week 8-11:  Phase 4 (AI強化) ← 最大価値
Week 12-17: Phase 5 (外部連携)
Week 18-20: Phase 6 (管理機能)
Week 21-23: Phase 7 (ヘルススコア)
```

---

## 🛠️ 技術的考慮事項

### 1. パフォーマンス

| 課題 | 対策 |
|------|------|
| BigQuery クエリ遅延 | キャッシュ層追加 (Redis / Cloud Memorystore) |
| 大量タスク表示 | 仮想スクロール (react-window) |
| AI処理時間 | 非同期処理 + WebSocket通知 |

### 2. スケーラビリティ

```
現在のアーキテクチャはCloud Run + BigQueryでスケーラブル
追加検討:
- Cloud Scheduler: 定期レポート生成
- Cloud Tasks: 重い処理のキューイング
- Firestore: リアルタイム更新 (オプション)
```

### 3. セキュリティ

| 要件 | 実装 |
|------|------|
| 認証 | Google OAuth (実装済み) |
| 認可 | ロールベースアクセス制御 (Phase 6) |
| データ分離 | tenant_id によるマルチテナント |
| 監査 | 操作ログ記録 (Phase 6) |

### 4. AI コスト最適化

```
Gemini API 使用量削減戦略:
1. プロンプトキャッシュ (同一議事録の再解析防止)
2. バッチ処理 (複数ファイルをまとめて処理)
3. モデル選択 (軽量タスクは gemini-flash 使用)
4. レスポンスサイズ制限 (JSON Schema制約)
```

---

## 📊 優先度マトリクス

```
        高インパクト
             │
    Phase 4  │  Phase 1
    (AI強化) │  (ダッシュボード)
             │
             │  Phase 2
             │  (レポート)
─────────────┼─────────────
             │
    Phase 5  │  Phase 3
    (連携)   │  (差分検出)
             │
             │  Phase 6, 7
             │  (管理・スコア)
             │
        低インパクト

     高工数 ←──────────→ 低工数
```

**推奨**: Phase 1 → 2 → 4 の順で、短期間で高価値を実現

---

## 📝 今後のアクション

### 継続的な改善
1. [x] Phase 1-7 の基本機能を実装完了
2. [ ] ユーザーフィードバックに基づく改善
3. [ ] パフォーマンス最適化
4. [ ] 追加連携機能の検討（Google Meet, Zoom, Teams）

### 検討中の機能
- フィードバックループ（AI学習の改善）
- テナント設定のカスタマイズ
- データ保持ポリシーの自動化
- アラート設定の詳細化

---

*Last Updated: 2025-12-08*

