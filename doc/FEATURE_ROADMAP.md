# Feature Roadmap - Project Progress DB

## 📋 目次

1. [ビジョンと目標](#ビジョンと目標)
2. [現在の実装状況](#現在の実装状況)
3. [機能一覧（フェーズ別）](#機能一覧フェーズ別)
4. [開発の進め方](#開発の進め方)
5. [技術的考慮事項](#技術的考慮事項)

---

## 🎯 ビジョンと目標

### ミッション
> 会議議事録という非構造化データをAIで構造化し、プロジェクト管理の生産性を10倍向上させる

### 成功指標（KPI）
- 会議後のタスク整理時間: 30分 → 3分（90%削減）
- リスク見落とし率: 50%削減
- プロジェクト状況把握時間: 5分以内で全体俯瞰

---

## ✅ 現在の実装状況

### 実装済み機能 (MVP)

| カテゴリ | 機能 | 状態 | 備考 |
|----------|------|------|------|
| **認証** | Google OAuth | ✅ 完了 | ドメイン制限対応 |
| **認証** | セッション管理 (JWT) | ✅ 完了 | |
| **入力** | 議事録アップロード (.txt/.md) | ✅ 完了 | |
| **入力** | 会議メタ情報入力 | ✅ 完了 | 日付・タイトル |
| **AI解析** | プロジェクト抽出 | ✅ 完了 | Gemini 2.5 Pro |
| **AI解析** | タスク抽出 | ✅ 完了 | 担当者・期限・優先度・ステータス |
| **AI解析** | リスク抽出 | ✅ 完了 | レベル・元発言保持 |
| **AI解析** | 決定事項抽出 | ✅ 完了 | |
| **AI解析** | 自然言語日付解析 | ✅ 完了 | 「来週金曜」→絶対日付 |
| **表示** | プロジェクト一覧 | ✅ 完了 | |
| **表示** | タスク一覧 | ✅ 完了 | フィルタ・ソート・ページネーション |
| **表示** | リスク一覧 | ✅ 完了 | 統計サマリー・多軸フィルタ |
| **表示** | 決定事項一覧 | ✅ 完了 | |
| **表示** | 会議一覧 | ✅ 完了 | |
| **編集** | タスク編集・削除 | ✅ 完了 | ソフトデリート |
| **編集** | リスク編集・削除 | ✅ 完了 | |
| **編集** | 決定事項編集・削除 | ✅ 完了 | |
| **出力** | CSV エクスポート | ✅ 完了 | プロジェクト/タスク/リスク/決定事項 |

---

## 📦 機能一覧（フェーズ別）

### Phase 1: ダッシュボード強化（1-2週間）

**目標**: 日常的に使いたくなるダッシュボードに進化

| # | 機能名 | 説明 | 優先度 | 工数 |
|---|--------|------|--------|------|
| 1.1 | **遅延タスクハイライト** | 期限切れタスクを赤で強調表示、専用ビュー | 高 | 0.5日 |
| 1.2 | **今週期限タスクビュー** | 今週中に期限が来るタスクの専用ビュー | 高 | 0.5日 |
| 1.3 | **担当者別タスクビュー** | 担当者でグルーピングしたタスク表示 | 中 | 1日 |
| 1.4 | **プロジェクトサマリー強化** | 未完了タスク数、遅延数、リスク数をカードに表示 | 高 | 1日 |
| 1.5 | **検索機能強化** | グローバル検索（タスク/リスク/決定事項横断） | 中 | 1日 |
| 1.6 | **ダッシュボードカスタマイズ** | ウィジェットの表示/非表示、並び替え | 低 | 2日 |

**実装アプローチ**:
```
frontend/
├── components/
│   ├── OverdueTasksWidget.tsx      # 1.1
│   ├── DueThisWeekWidget.tsx       # 1.2
│   ├── TasksByOwnerView.tsx        # 1.3
│   ├── ProjectSummaryCard.tsx      # 1.4 (既存拡張)
│   └── GlobalSearch.tsx            # 1.5
└── app/
    └── dashboard/
        └── page.tsx                # カスタマイズ可能なダッシュボード
```

---

### Phase 2: 週次レポート機能（1-2週間）

**目標**: PMO/マネージャー向けのレポート自動生成

| # | 機能名 | 説明 | 優先度 | 工数 |
|---|--------|------|--------|------|
| 2.1 | **週次サマリー生成** | 今週の遅延/リスク/決定事項をAIで要約 | 高 | 2日 |
| 2.2 | **Top10 遅延/リスクメール** | コピペ可能なメール文面を自動生成 | 高 | 1日 |
| 2.3 | **レポートテンプレート** | カスタマイズ可能なレポート形式 | 中 | 1日 |
| 2.4 | **定期レポート配信** | 週次で自動生成・通知 | 低 | 2日 |

**実装アプローチ**:
```
backend/api/routers/
└── reports.py                      # 新規: レポート生成API

frontend/app/
└── reports/
    ├── page.tsx                    # レポート一覧・生成画面
    └── weekly/
        └── page.tsx                # 週次レポート詳細

backend/worker/
└── services/
    └── report_generator.py         # AIレポート生成ロジック
```

**APIエンドポイント**:
```
POST /reports/weekly/generate       # 週次レポート生成
GET  /reports/weekly/{week}         # 特定週のレポート取得
GET  /reports/email-draft           # メールドラフト生成
```

---

### Phase 3: 会議間差分検出（2-3週間）

**目標**: 前回会議からの変化を自動検出

| # | 機能名 | 説明 | 優先度 | 工数 |
|---|--------|------|--------|------|
| 3.1 | **新規タスク検出** | 前回会議後に追加されたタスク | 高 | 2日 |
| 3.2 | **ステータス変更検出** | IN_PROGRESS→DONEなどの変化 | 高 | 2日 |
| 3.3 | **エスカレーションリスク** | LOW→HIGHに上がったリスク | 高 | 1日 |
| 3.4 | **差分ビュー** | 会議間の変化を視覚的に表示 | 中 | 2日 |
| 3.5 | **タスクライフサイクル** | タスクの初出→議論→完了の履歴 | 中 | 3日 |

**データモデル拡張**:
```sql
-- 履歴テーブル追加
CREATE TABLE task_history (
    history_id STRING,
    task_id STRING,
    meeting_id STRING,
    field_changed STRING,
    old_value STRING,
    new_value STRING,
    changed_at TIMESTAMP
);

CREATE TABLE risk_history (
    history_id STRING,
    risk_id STRING,
    meeting_id STRING,
    old_level STRING,
    new_level STRING,
    changed_at TIMESTAMP
);
```

---

### Phase 4: AI強化（3-4週間）

**目標**: AIをより賢く、対話的に

| # | 機能名 | 説明 | 優先度 | 工数 |
|---|--------|------|--------|------|
| 4.1 | **自然言語クエリ** | 「Project Aの今月期限タスクを見せて」→結果表示 | 高 | 5日 |
| 4.2 | **AIチャットボット** | ダッシュボード内で質問→回答 | 高 | 5日 |
| 4.3 | **次回アジェンダ提案** | 未解決事項からアジェンダ自動生成 | 中 | 3日 |
| 4.4 | **リスクスコアリング** | 曖昧な発言のリスク度を0-1でスコア化 | 中 | 3日 |
| 4.5 | **ボトルネック分析** | 「このプロジェクトの問題点は？」への回答 | 低 | 3日 |
| 4.6 | **フィードバックループ** | ユーザー訂正から学習 | 低 | 5日 |

**実装アプローチ**:
```
frontend/components/
├── AIChatWidget.tsx               # チャットUI
└── NaturalLanguageSearch.tsx      # 自然言語検索バー

backend/api/routers/
└── ai.py                          # AI対話API

backend/worker/services/
├── nl_query_parser.py             # 自然言語→SQLクエリ変換
├── agenda_generator.py            # アジェンダ生成
└── risk_scorer.py                 # リスクスコアリング
```

**APIエンドポイント**:
```
POST /ai/query                     # 自然言語クエリ
POST /ai/chat                      # チャット対話
POST /ai/agenda/generate           # アジェンダ生成
GET  /ai/analysis/bottleneck       # ボトルネック分析
```

---

### Phase 5: 外部連携（4-6週間）

**目標**: 会議データの自動取り込み

| # | 機能名 | 説明 | 優先度 | 工数 |
|---|--------|------|--------|------|
| 5.1 | **Google Drive連携** | ドライブから議事録をインポート | 高 | 3日 |
| 5.2 | **Google Docs連携** | Docsから直接テキスト取得 | 高 | 2日 |
| 5.3 | **Google Calendar連携** | 会議情報の自動取得 | 中 | 2日 |
| 5.4 | **Google Meet文字起こし** | Meet録画の文字起こし取り込み | 低 | 5日 |
| 5.5 | **Zoom連携** | Zoom文字起こし取り込み | 低 | 5日 |
| 5.6 | **Slack通知** | 高リスク・遅延タスク通知 | 中 | 3日 |
| 5.7 | **Teams通知** | Teams向け通知 | 低 | 3日 |

**実装アプローチ**:
```
backend/api/
├── integrations/
│   ├── google_drive.py
│   ├── google_docs.py
│   ├── google_calendar.py
│   ├── google_meet.py
│   ├── zoom.py
│   └── slack.py
└── routers/
    └── integrations.py            # 連携設定API

frontend/app/
└── settings/
    └── integrations/
        └── page.tsx               # 連携設定画面
```

---

### Phase 6: 管理機能（2-3週間）

**目標**: 企業利用に必要な管理機能

| # | 機能名 | 説明 | 優先度 | 工数 |
|---|--------|------|--------|------|
| 6.1 | **ロール管理** | Admin / PM / Member の権限分離 | 高 | 3日 |
| 6.2 | **ユーザー管理** | ユーザー招待・削除・権限変更 | 高 | 2日 |
| 6.3 | **監査ログ** | 操作履歴の記録・表示 | 中 | 2日 |
| 6.4 | **テナント設定** | 用語カスタマイズ、リスクレベル定義 | 中 | 2日 |
| 6.5 | **データ保持ポリシー** | 古いデータの自動アーカイブ | 低 | 2日 |

**データモデル拡張**:
```sql
CREATE TABLE users (
    user_id STRING,
    tenant_id STRING,
    email STRING,
    role STRING,  -- 'admin', 'pm', 'member'
    created_at TIMESTAMP
);

CREATE TABLE audit_logs (
    log_id STRING,
    tenant_id STRING,
    user_id STRING,
    action STRING,
    resource_type STRING,
    resource_id STRING,
    details JSON,
    created_at TIMESTAMP
);
```

---

### Phase 7: ヘルススコア（2-3週間）

**目標**: プロジェクトの健全性を数値化

| # | 機能名 | 説明 | 優先度 | 工数 |
|---|--------|------|--------|------|
| 7.1 | **プロジェクトヘルススコア** | 遅延率・リスク数から健全性を計算 | 高 | 3日 |
| 7.2 | **トレンド分析** | 過去n週のスコア推移 | 中 | 2日 |
| 7.3 | **アラート設定** | スコア低下時の通知 | 中 | 2日 |
| 7.4 | **比較ビュー** | 複数プロジェクトのスコア比較 | 低 | 2日 |

**スコア計算ロジック例**:
```python
def calculate_health_score(project_id: str) -> float:
    """
    ヘルススコア計算 (0-100)
    - 遅延タスク率: -20点 (50%以上で最大減点)
    - 高リスク数: -5点/件
    - 未完了タスク率: -10点 (70%以上で最大減点)
    - 最終更新からの日数: -1点/日 (7日以上で減点開始)
    """
    base_score = 100
    
    overdue_penalty = min(20, overdue_rate * 40)
    high_risk_penalty = min(25, high_risk_count * 5)
    uncompleted_penalty = min(10, max(0, uncompleted_rate - 0.3) * 33)
    stale_penalty = min(15, max(0, days_since_update - 7))
    
    return max(0, base_score - overdue_penalty - high_risk_penalty 
               - uncompleted_penalty - stale_penalty)
```

---

## 🔧 開発の進め方

### 1. 開発フロー

```
1. Issue作成 (機能ごと)
   ↓
2. ブランチ作成 (feature/xxx)
   ↓
3. 実装
   ├── Backend API
   ├── Frontend UI
   └── Tests
   ↓
4. PR → レビュー → マージ
   ↓
5. Staging デプロイ → 動作確認
   ↓
6. Production デプロイ
```

### 2. 開発環境セットアップ

```bash
# Backend
cd backend/api
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
uvicorn main:app --reload --port 8000

# Frontend
cd frontend
npm install
cp .env.example .env.local
npm run dev
```

### 3. テスト戦略

| レイヤー | ツール | カバレッジ目標 |
|----------|--------|----------------|
| Unit (Backend) | pytest | 80% |
| Unit (Frontend) | Jest | 70% |
| Integration | pytest + httpx | 重要フロー |
| E2E | Playwright | ハッピーパス |

### 4. CI/CD パイプライン

```yaml
# 既存の cloudbuild.yaml を活用
triggers:
  - main ブランチ: Production デプロイ
  - staging ブランチ: Staging デプロイ
  - PR: テスト実行のみ
```

### 5. 推奨開発順序

```
Week 1-2:   Phase 1 (ダッシュボード強化)
Week 3-4:   Phase 2 (週次レポート)
Week 5-7:   Phase 3 (会議間差分)
Week 8-11:  Phase 4 (AI強化) ← 最大価値
Week 12-17: Phase 5 (外部連携)
Week 18-20: Phase 6 (管理機能)
Week 21-23: Phase 7 (ヘルススコア)
```

---

## 🛠️ 技術的考慮事項

### 1. パフォーマンス

| 課題 | 対策 |
|------|------|
| BigQuery クエリ遅延 | キャッシュ層追加 (Redis / Cloud Memorystore) |
| 大量タスク表示 | 仮想スクロール (react-window) |
| AI処理時間 | 非同期処理 + WebSocket通知 |

### 2. スケーラビリティ

```
現在のアーキテクチャはCloud Run + BigQueryでスケーラブル
追加検討:
- Cloud Scheduler: 定期レポート生成
- Cloud Tasks: 重い処理のキューイング
- Firestore: リアルタイム更新 (オプション)
```

### 3. セキュリティ

| 要件 | 実装 |
|------|------|
| 認証 | Google OAuth (実装済み) |
| 認可 | ロールベースアクセス制御 (Phase 6) |
| データ分離 | tenant_id によるマルチテナント |
| 監査 | 操作ログ記録 (Phase 6) |

### 4. AI コスト最適化

```
Gemini API 使用量削減戦略:
1. プロンプトキャッシュ (同一議事録の再解析防止)
2. バッチ処理 (複数ファイルをまとめて処理)
3. モデル選択 (軽量タスクは gemini-flash 使用)
4. レスポンスサイズ制限 (JSON Schema制約)
```

---

## 📊 優先度マトリクス

```
        高インパクト
             │
    Phase 4  │  Phase 1
    (AI強化) │  (ダッシュボード)
             │
             │  Phase 2
             │  (レポート)
─────────────┼─────────────
             │
    Phase 5  │  Phase 3
    (連携)   │  (差分検出)
             │
             │  Phase 6, 7
             │  (管理・スコア)
             │
        低インパクト

     高工数 ←──────────→ 低工数
```

**推奨**: Phase 1 → 2 → 4 の順で、短期間で高価値を実現

---

## 📝 次のアクション

1. [ ] Phase 1 の Issue を作成
2. [ ] 開発環境の確認・整備
3. [ ] 遅延タスクハイライトから着手
4. [ ] 週次スプリントレビューを設定

---

*Last Updated: 2025-12-07*

