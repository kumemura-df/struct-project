# Worker Service Environment Variables

# GCP Configuration
PROJECT_ID=your-gcp-project-id
BIGQUERY_DATASET=project_progress_db
REGION=asia-northeast1

# Server Configuration (for Cloud Run HTTP endpoint)
PORT=8080
